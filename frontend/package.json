{
  "name": "front",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "light-server -s ./src -p 9090 -x http://localhost:9090 --proxypath \"/routes\" -w \"src/**/*.js, src/**/*.html\" -w \"src/**/*.css\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "light-server": "^2.9.1"
  }
}
